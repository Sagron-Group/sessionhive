import 'package:flutter/material.dart';
import 'package:nowa_runtime/nowa_runtime.dart';

@NowaGenerated()
final ThemeData lightTheme = ThemeData(
    colorScheme: const ColorScheme.light(
      primary: Color(0xffede338),
      secondary: Color(0xfffff28d),
      onSurface: Color(0xff39454a),
      surface: Color(0xff1d2528),
    ),
    textTheme: const TextTheme(
      bodyMedium: TextStyle(color: Color(0xffffff00)),
      bodyLarge: TextStyle(color: Color(0xffffffff)),
      titleLarge: TextStyle(color: Color(0xffffef00)),
    ));

@NowaGenerated()
final ThemeData darkTheme = ThemeData(
    colorScheme: const ColorScheme.dark(), textTheme: const TextTheme());
